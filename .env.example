# Server (set these in Render dashboard)
PORT=3001
JWT_SECRET=change-me-to-a-long-random-string
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Client (set VITE_API_URL in Vercel dashboard)
# Local dev uses proxy so this isn't needed locally
# VITE_API_URL=https://your-toasterai-api.onrender.com/api
